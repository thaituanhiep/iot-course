#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("size of char: %d bit\n", sizeof(char));
    printf("size of int: %d bit\n", sizeof(int));
    printf("size of long: %d bit\n", sizeof(long));
    printf("size of long long: %d bit\n", sizeof(long long));
    printf("size of float: %d bit\n", sizeof(float));
    printf("size of double: %d bit\n", sizeof(double));
    return 0;
}
